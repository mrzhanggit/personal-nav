# 静态站点混合托管方案

> GitHub Pages 托管轻量纯静态站 · Cloudflare Pages 托管图片多、体积大的站
> 2026-09-08 由小满整理并预配置

---

## 一、总体架构

| 站点 | 体积 | 托管平台 | 线上地址 |
|---|---|---|---|
| 个人学习导航页（含 Python 课程、历史时间线、Claude Code/CLI 课程） | 5.6 MB | **GitHub Pages** | `http://chengbin.vip/personal-nav/`（域名已绑定 ✓） |
| RWH 金句长图全集（1106+ 张长图） | 906 MB | **Cloudflare Pages** | `https://rwh-quotes.pages.dev`（待绑 `rwh.chengbin.vip`） |
| 词族记忆 word-memory | 小 | GitHub Pages（已有） | `https://mrzhanggit.github.io/word-memory/` |

分工逻辑：
- **GitHub Pages**：站点 < 1 GB、流量不大 → 免费够用，和 git 仓库天然一体
- **Cloudflare Pages**：单站点 1 GB 也没关系（免费版不限制总容量和带宽）→ 图片大户专用
- 其他已部署在 WorkBuddy 沙箱的小站（资产看板、习惯力等）维持现状即可，想迁移随时可加

---

## 二、GitHub Pages 部署（personal-nav）— 已完成 ✓

实际发布方式：**legacy 模式，发布源为 `gh-pages` 分支**，线上地址 `http://chengbin.vip/personal-nav/`（自定义域名已绑定，DNS 在阿里云）。

日常更新流程（已验证可用）：

```bash
cd ~/Downloads/日常工作/personal-nav
git checkout main                          # 在 main 上改 links.json / dist/
# ... 修改 links.json（根目录 + dist 两份同步）并 commit ...

# 发布：把 dist/ 同步到 gh-pages 分支
git checkout gh-pages
find . -maxdepth 1 -mindepth 1 -not -name ".git" -exec rm -rf {} +
git archive main dist | tar -x --strip-components=1
git add -A && git commit -m "同步 dist"
git push origin gh-pages                   # Pages 自动构建，约 1-2 分钟生效
git checkout main

# 别忘了 main 也要推，保持镜像仓库一致
git push origin main
```

> GitHub 授权已由小满通过设备码流程完成（gh CLI 已登录 mrzhanggit，凭证走 keyring）。
> 仓库内另有一份 `.github/workflows/deploy-pages.yml`（Actions 部署方案），当前 legacy 模式下不会触发，仅作备用。

---

## 三、Cloudflare Pages 部署（RWH 金句长图全集）

已预配置完成：
- ✅ `deploy-cloudflare.sh` — 一键直传部署脚本
- ✅ `.wranglerignore` — 排除脚本和系统文件

已验证兼容性：1365 个文件（限 20000）✓ · 最大单文件 3.94 MB（限 25 MB）✓ · 全部相对路径 ✓

您需要做的（只需一次）：

```bash
# 1. 登录 Cloudflare（浏览器授权，需要 Cloudflare 账号，免费注册即可）
cd ~/Desktop/日常工作/RWH-金句长图全集
npx wrangler login

# 2. 首次部署（906MB 上传需要一些时间，走代理可在脚本里取消注释）
./deploy-cloudflare.sh
```

部署完成后访问：`https://rwh-quotes.pages.dev`

日常更新：往目录里加新的 RWH 期数图片 → 重跑 `./deploy-cloudflare.sh`（增量上传，只传变化部分）。

---

## 四、自定义域名绑定

张总已有域名 **chengbin.vip**（DNS 托管在阿里云 hichina.com），导航页已绑定 `chengbin.vip/personal-nav/`。

### 导航页（GitHub Pages）— 已完成 ✓

- 仓库 Settings → Pages 已绑定自定义域名，发布源为 `gh-pages` 分支
- 更新站点：把 `dist/` 内容同步到 `gh-pages` 分支推送即可（本文档第六节有完整命令）

### Cloudflare Pages 绑定 `rwh.chengbin.vip`（RWH 部署后操作）

1. **阿里云 DNS 控制台**（dns.hichina.com）→ chengbin.vip → 解析设置 → 添加记录：

   | 记录类型 | 主机记录 | 记录值 |
   |---|---|---|
   | CNAME | rwh | `rwh-quotes.pages.dev` |

2. **Cloudflare Dashboard** → Pages → `rwh-quotes` 项目 → Custom domains → 添加 `rwh.chengbin.vip` → 等待验证生效
3. 验证通过后 `https://rwh.chengbin.vip` 即可访问（Cloudflare 自动签发 SSL 证书）

### 绑定后同步改导航

在 `links.json` 里把对应条目的 `url` 换成新地址，两份（根目录 + dist）都要改，然后走第六节发布流程。

---

## 五、跨平台资源引用规则（黄金法则）

两边平台、两种 URL 结构（GitHub Pages 是子路径 `/personal-nav/`，Cloudflare 是根路径），路径处理记住三条：

1. **站内引用一律相对路径**——不要用 `/` 开头
   - ✅ `href="timeline/rome/index.html"`
   - ✅ `src="./assets/pic.png"`
   - ❌ `href="/timeline/rome/index.html"`（GitHub Pages 子路径下会 404）
   - 已全站检查过现有页面：零违规 ✓

2. **跨站链接一律完整 URL**——从导航页跳到 RWH、资产看板等，写完整 `https://...`，与托管平台无关

3. **`links.json` 是导航唯一数据源**，维护约定：
   - 根目录 `links.json` 与 `dist/links.json` 必须同步修改（内容应保持一致）
   - `url` 字段：站内条目用相对路径，外部条目用完整 URL
   - 改完 commit + push 即自动发布 GitHub Pages

4. **CDN / 外链资源**（字体、JS 库）不受平台影响，可正常使用（现有 japan-history.html 引了外部 CDN，两边都能跑）

---

## 六、待办与已知事项

- [x] ~~GitHub 授权 + 启用 Pages + push~~（2026-09-08 完成，gh CLI 已登录，gh-pages 已同步新导航条目）
- [ ] `npx wrangler login` → `./deploy-cloudflare.sh`（第三节，需要张总在浏览器点一次 Allow）
- [ ] RWH 部署成功后：把导航里指向 `e24a9bd3...gz1.agentos-app.net` 的条目改为新的 Cloudflare 地址（该沙箱目前同时挂着旧播客站，内容已复用，正好借此理清）
- workbuddy.link 域名 2026-09-08 全系 502（平台现象），槽边往事等条目暂指向原地址，恢复后自动可用
